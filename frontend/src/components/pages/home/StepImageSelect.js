import React from "react";




export default function StepImageSelect(props)
{


    return (
        <div>
            <p>
                StepImageSelect, Active: {props.currentStep}
                Is Active: {props.isActive.toString()}
            </p>
            <p>
                StepImageSelect, Active: {props.currentStep}
                Is Active: {props.isActive.toString()}
            </p>
            <p>
                StepImageSelect, Active: {props.currentStep}
                Is Active: {props.isActive.toString()}
            </p>

            <p>
                StepImageSelect, Active: {props.currentStep}
                Is Active: {props.isActive.toString()}
            </p>



        </div>
    );
}